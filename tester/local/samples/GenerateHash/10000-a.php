<?php
/*
$uniqueHash = [];

for($a = 0; $a < 10; $a++) {
	for($b = 0; $b < 1000; $b++) {
		$editionHash = '';

		for($i = 0; $i < 12; $i++) {
			$editionHash .= mt_rand(0, 9);
		}

		if (!in_array($editionHash, $uniqueHash)) {
			$uniqueHash[] = $editionHash;
		}
	}
}
*/